# Supabase project that hosts the culture_articles table and the two edge functions.
# Without these the app still loads and the local library works, but generating
# new concepts is disabled.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
